[project]
name = "top-tier-coding-agent"
version = "0.1.0"
dependencies = [
  "fastapi>=0.116.0",
  "uvicorn>=0.35.0",
  "pydantic>=2.11.0",
  "requests>=2.32.0"
]

[project.optional-dependencies]
dev = ["pytest>=8.4.0"]

[tool.pytest.ini_options]
pythonpath = ["src"]
